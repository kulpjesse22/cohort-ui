import { DemoTour } from "@/components/DemoTour";

export const metadata = {
  title: "Cohort — guided tour",
  description: "How a team of agents plans, builds, reviews, and grows.",
};

export default function DemoPage() {
  return <DemoTour />;
}
